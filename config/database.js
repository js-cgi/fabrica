export const database = {
    driver: "none",

    sqlite: {
        path: "./storage/app.db"
    },

    mysql: {
        host: "localhost",
        port: 3306,
        user: "root",
        password: "",
        database: "fabrica"
    }
};
